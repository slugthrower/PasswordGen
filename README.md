Java source code for a printable ASCII password generator. Currently only runs from the command line, the program will
prompt the user for how many passwords and how many bits per password to generate.
